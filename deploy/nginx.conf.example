server {
    listen 80;
    server_name infinitylinks.fun;

    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name infinitylinks.fun;

    # Certbot can manage these paths after you issue a certificate.
    ssl_certificate /etc/letsencrypt/live/infinitylinks.fun/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/infinitylinks.fun/privkey.pem;

    location = /api/sync {
        client_max_body_size 5m;
        proxy_pass http://127.0.0.1:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        proxy_pass http://127.0.0.1:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
